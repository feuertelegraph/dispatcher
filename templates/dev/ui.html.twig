{% import "macros/button.html.twig" as button  %}

<div class="min-h-screen">
    {{ button.button("Submit", "primary", "button") }}
    {{ button.button("Submit", "primary-outline", "button") }}
    {{ button.button("Submit", "success", "button") }}
    {{ button.button("Submit", "info", "button") }}
    {{ button.button("Submit", "warning", "button") }}
    {{ button.button("Submit", "danger", "button") }}

    {{ button.infobox("This is success", "succsess") }}

    {% include 'macros/modal.html.twig' %}
</div>
<table class="table">
    <thead>
    <tr>
        <th>Textspalte mit Link</th>
        <th>Textspalte</th>
        <th>Zahlen</th>
        <th>Spalte mit Status</th>
        <th>Spalte mit Bild</th>
        <th>Spalte mit Button</th>
    </tr>
    </thead>
    <tbody>
    {% for i in range(1, 5) %}
        <tr>
            <td>
                <a href="#" class="link">Link</a>
            </td>
            <td>Label</td>
            <td class="text-right">1.234,56</td>
            <td>Status</td>
            <td class="flex justify-center items-center"><img class="h-32" src="https://picsum.photos/200"></td>
            <td>
                <button class="btn">Button</button>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>